-- Share-link repaint: paint companies the sharing retailer opened up for the
-- viewer's repaint palette (comma-separated brand names). Null = every brand.
ALTER TABLE projects ADD COLUMN share_brands text;
